spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: puni_db
  application:
    name: journalservice
  kafka:
    bootstrap-servers: localhost:29092
eureka:
  client:
    service-url:
      defalutZone: http://localhost:8761/eureka/

server:
  port: 9899

